{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  //adding the connection-string  between the ssms and the Visual Studio
  "ConnectionStrings": {
    "FullStackConnectionString": "server=(localdb)\\MSSQLLocalDB;database=newoneDb;Trusted_Connection=true"
  }

}
